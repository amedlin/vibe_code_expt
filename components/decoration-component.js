class DecorationComponent {
    constructor(type, depthLayer, scale = 1, depthOffset = 0) {
        this.type = type;
        this.depthLayer = depthLayer;
        this.scale = scale;
        this.depthOffset = depthOffset;
    }
}
