// PlatformComponent — marker component for level platforms.
//
// Carries no state; serves as the entity tag the PlatformRenderSystem
// targets when drawing the level's platforms through the active theme.
class PlatformComponent {
    constructor() {}
}
